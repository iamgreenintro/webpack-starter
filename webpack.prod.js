const path = require('path');

module.exports = {
  // This configuration contains specific settings for 'production' mode.
  mode: 'production',

  // Define entry point(s):
  entry: {
    main: {},
  },
};
